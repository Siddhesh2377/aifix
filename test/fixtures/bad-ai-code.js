// This file intentionally contains AI-generated code mistakes for testing

const express = require("express");
const nonexistent_package = require("hallucinated-ai-lib"); // hallucinated import

// AI left console.logs everywhere
const data = fetch("/api/users");
console.log("data", data);

// No error handling on fetch
async function getUsers() {
  const response = await fetch("/api/users");
  const data = response.json();
  return data;
}

// Empty catch block
try {
  JSON.parse(userInput);
} catch (e) {}

// AI placeholder left in
const API_KEY = "your_api_key";
// TODO: implement authentication

// Hardcoded secret
const token = "sk-1234567890abcdefghijklmnop";

// Dead code after return
function calculate(x) {
  return x * 2;
  const unused = x + 1;
}

// As an AI, I generated this code
// Add your actual implementation here

// Commented out code block
// const oldHandler = (req, res) => {
// const result = await db.query("SELECT * FROM users");
// return res.json(result);
// };

const result = fetch("/api/data").then(r => r.json());
